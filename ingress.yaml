apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress
spec:
  rules: 
    - http: 
        paths: /
        pathType: ImplementationSpecific 
        backend: 
          service: 
            name: python-app-svc 
            port: 
              number: 5000
